"""
Ctrl+d 复制上一行到下一行，   Ctrl+y 删除一行，   shift+Enter 快速回到下一行行首， Ctrl+/  批量注释和取消批量注释；
选中几行代码 按Tab 键缩进， 按shift+tab 取消缩进 ctrl+- 折叠代码 Ctrl+shift 全部展开代码  ctrl+p 调出可用参数
按Ait 键 鼠标左键选几行，可以对选中的几行同时输入字符，
按ctrl 鼠标左键点击 python 模块名 可显示模块代码内容
python中使用DocStrings ： 注释 在定义函数时 第二行 输完三对英文双引号 按回车键 加入注释 变量说明
"""Python中的UTF-8编码转换方法  


"""

"""
# int 整数
# string 字符串
# float 浮点数
# type  查看数据类型
#  %取余数 // 取整数 **指数
# a += b  把等号右边的结果赋值给左面变量  a = a+b
# 如果字符串里面含有双引号，可用单引号包围，含有单引号用双引号包围，或者用右斜杠\ 接触引号效能（转义符）

# 表达式：一条具有明确执行结果的代码语句，

# 布尔（bool） True 真 False 假 ==等于 !=不等于 < 小于 >大于 >=大于等于 <=小于等于 满足返回True 否则返回False
# if 要判断的条件：
#     条件成立时要做的事情；   注意：条件后要加冒号，第二行要缩进四个空格，表明归属关系
# else: 判断条件不成立时执行;不缩进，
# elif 多条件判断，语法 if 条件1：   多条件判断下，条件是互斥的，即符合第一个条件的，第二个条件就无用了。
#                       elif 条件2：
"""
*********************数据类型  type()  isinstance()**********************************
一：type() 方法的语法: 
    1）type(object) ，返回值 一个参数返回对象类型
    2）type(name, bases, dict)；  返回值 三个参数，返回新的类型对象
      参数   name -- 类的名称。 bases -- 基类的元组。 dict -- 字典，类内定义的命名空间变量。
   --------------------------------------------------------------------------------------
二： isinstance() 函数来判断一个对象是否是一个已知的类型，类似 type()。
    isinstance() 与 type() 区别：  type() 不会认为子类是一种父类类型，不考虑继承关系。
                                  isinstance() 会认为子类是一种父类类型，考虑继承关系。
    语法：isinstance(object, classinfo)  如果对象的类型与参数二的类型（classinfo）相同则返回 True，否则返回 False
       object -- 实例对象。
       classinfo -- 可以是直接或间接类名、基本类型或者由它们组成的元组。
    如果要判断两个类型是否相同推荐使用 isinstance()。
**********************************字符串 转义字符 格式化 *********************************
一： 转义字符 在需要在字符中使用特殊字符时，python 用反斜杠 \ 转义字符。如下表：
        转义字符        	描述
        \(在行尾时)     	续行符
        \\	            反斜杠符号
        \'	            单引号
        \"	            双引号
        \a	            响铃
        \b	            退格(Backspace)
        \e	            转义
        \000	        空
        \n	            换行
        \v	            纵向制表符
        \t	            横向制表符
        \r	            回车
        \f	            换页
        \oyy	        八进制数，y 代表 0~7 的字符，例如：\012 代表换行。
        \xyy	        十六进制数，以 \x 开头，yy代表的字符，例如：\x0a代表换行
        \other	        其它的字符以普通格式输出
二：Python字符串运算符  下表实例变量 a 值为字符串 "Hello"，b 变量值为 "Python"：
    操作符	描述                      	实例
    +	    字符串连接	                >>>a + b    'HelloPython'
    *	    重复输出字符串	            >>>a * 2    'HelloHello'
    []	    通过索引获取字符串中字符	    >>>a[1]     'e'
    [ : ]	截取字符串中的一部分	        >>>a[1:4]   'ell'
    in	    成员运算符 - 如果字符串中包含给定的字符返回 True	>>>"H" in a             True
    not in	成员运算符 - 如果字符串中不包含给定的字符返回 True	>>>"M" not in a     True
    r/R 	原始字符串 - 原始字符串：所有的字符串都是直接按照字面的意思来使用，没有转义特殊或不能打印的字符。 
            原始字符串除在字符串的第一个引号前加上字母"r"（可以大小写）以外，与普通字符串有着几乎完全相同的语法。	
            >>>print r'\n'   \n
            >>> print R'\n'   \n
    %	格式字符串	请看下面
三： 1）python 字符串格式化符号:
          符号	 描述
          %c	 格式化字符及其ASCII码
          %s	 格式化字符串
          %d	 格式化整数
          %u	 格式化无符号整型
          %o	 格式化无符号八进制数
          %x	 格式化无符号十六进制数
          %X	 格式化无符号十六进制数（大写）
          %f	 格式化浮点数字，可指定小数点后的精度
          %e	 用科学计数法格式化浮点数
          %E	 作用同%e，用科学计数法格式化浮点数
          %g	 %f和%e的简写
          %G	 %F 和 %E 的简写
          %p	 用十六进制数格式化变量的地址
    2）格式化操作符辅助指令:
            符号	    功能
            *	    定义宽度或者小数点精度
            -	    用做左对齐
            +	    在正数前面显示加号( + )
            <sp>	在正数前面显示空格
            #	    在八进制数前面显示零('0')，在十六进制前面显示'0x'或者'0X'(取决于用的是'x'还是'X')
            0	    显示的数字前面填充'0'而不是默认的空格
            %	    '%%'输出一个单一的'%'
            (var)	映射变量(字典参数)
            m.n.	m 是显示的最小总宽度,n 是小数点后的位数(如果可用的话)
            Python2.6 开始，新增了一种格式化字符串的函数 str.format()，它增强了字符串格式化的功能。
四：Python 三引号   中三引号可以将复杂的字符串进行赋值。
    Python 三引号允许一个字符串跨多行，字符串中可以包含换行符、制表符以及其他特殊字符。
    三引号的语法是一对连续的单引号或者双引号（通常都是成对的用）。
********************************  字符串格式化2*******************************
 字符格式化拼接 多个变量占位，变量要有括号括起来，并按照占位的顺序填入。
     示例：print("%d * %d = %d" %(i,j,k),end=" ")  输出 i*j=k 不换行结尾加空格（end=" ")
     %s 将内容装换成字符串放入占位位置，%d 将内容装换成整数，放入占位位置，%f 将内容转换成浮点型放入占位位置。
     m,控制数字宽度， 小于数字本身不生效，.n,控制小数精度，要求是数字，会进行小数四舍五入。
 
 字符串快速格式化  
     通用语法: f"内容{变量}"的方式快速格式化 不限数据类型，没有精度控制。
****************** 字符串格式：% 、str.format()、f-string、format()******************
一：%  语法："%[(name)][flags][width][.precison]type" % 待格式化数据
     参数：format()是python的一个内置函数，其使用频率不高，语法和str.format()大同小异，可以结合lambda函数使用或在其它一些特定情况下使用。
          (1)name:字典参数  不用字典参数时可以不写 直接写%d %s
          (2）flags:1）-: 左对齐，，正数不加，负数加-号
                    2)+: 右对齐，如果是正数，显示的数字前面有+号 ，负数加-号， 如：%+10 %25   #右对齐 +25
                    2) 空格: 右对齐(默认的对齐方式)，正数前加空格，负数前加负号;
                    3) 0: 右对齐，以0填充，正数无符号，负数加负号，并将符号放置在0最左侧;
         (3) width: 占位宽度, 若指定宽度小于原数据长度则按原长度数据输出;
         (4) .precison: 小数点后保留位数；在字符串中则表示截取/字符串切片;
         (5) type: 详见如下...
                (1) s: string, 字符串;
                (2) d: decimal integer, 十进制数;
                (3) l: integer, 用法同%d; 表示长整型（long integer） 在Python 3中所有的整数类型都是“长整型”
                (4) u: unsigned integer, 无符号十进制数;
                (5) f: float, 浮点数(默认保留小数点后6位);
                (6) F: Float, 浮点数(默认保留小数点后6位);
                (7) e: exponent, 将数字表示为科学计数法(小写e, 默认保留小数点后6位);
                (8) E: Exponent, 将数字表示为科学计数法(大写E, 默认保留小数点后6位);
                (9) o: octal, 八进制数(即0-7);
                (10) x: hexdecimal, 十六进制数(即0-9a-f);
                (11) X: Hexdecimal, 十六进进制数(0-9A-F);
                (12) g: general format, 通用格式，详见如下...;
                (13) G: General format, 通用格式，详见如下...;
                (14) %c: character, 将十进制数转换为所对应的unicode值;
                (15) %r: representation, 调用__repr__魔法方法输出;
                (16) %%: 转义%，输出百分号。
  二、str.format()格式化
    1.语法："{[index][:[[fill]align][sign][#][0][width][grouping_option][.precision][type]]}".format()
    2.参数详解
    (1) index: 待格式化字符的索引或键，若占位符数量和参数数量不一致时必须指定索引;
    (2) fill: 填充字符，可为任意字符;
    (3) align: 对齐方式(常配合width使用)，可选:
     # 和Excel中输入文本和数字的默认对齐方式一致
        1) <: 左对齐(字符串默认对齐方式);
        2) >: 右对齐(数字默认对齐方式);
        3) ^: 居中对齐;
        4) =: 内容右对齐，将符号(+或-)放置在填充字符的左侧，仅对数字类型有效;
    (4) sign: 有无符号，可选：
        1) +: 正数加正号，负数加负号；
        2) -: 正数不变，负数加负号(默认)；
        3) 空格: 正数加空格，负数加负号；
    (5) #:  
           a. 对于整数，在输出值分别添加响应的0b, 0o, 0x前缀;
           b. 对于浮点数和复数, 在输出值保留小数点符号;
           c. 在g/G模式下，保留末尾的0；
    (6) 0: 若未设置对齐方式，在width前加一个0将为数字类型启用感知正负号的零填充，等同于设置fill为0, align为"=";
    (7) width: 字段总宽度(十进制整数), 所有前缀，分隔符和其它格式化字符之和;  
    (8) grouping_option: 设置分组(分隔):
           1) ",": 使用逗号作为千位分隔符;
           2) "_": 使用_作为分隔符:
              a. 对于十进制数, 使用_作为千位分隔符;
              b. 对于b, o, x/X，使用_每4位数插入一个下划线；
    (9) .precision(十进制数):  
           a. 整数型不允许设置precison, 如果设置即被转换为浮点数;
           b. 浮点型表示小数点"后"显示多少位小数位数;
           c. 以g或G格式化表示在小数点"前后"共显示多少个数位;
           d. 字符型表示截取多少个字符；
    (10) {{或}}: 转义{或}，当需要输出{或}的使用使用;
    (11) type: 详见如下...
         format()格式化与%格式化的type大同小异，
        (1) b: binary, 二进制;
        (3) g: general formatting, 
        (4) G: General formatting, 
三、f-string格式化
    1.语法：f"{变量1}{变量2}{变量n}"
     python3.6以后开始支持f-string字符串。f-string即formatting string, 它是str.format()的一个变种，其语法形式之殊途同归
     很多时候使用f-string可以有效减少代码量，更为清晰易读
四、format()
1.语法：format(x, formatter) # x为需要格式化的数据，formatter为格式化表达式，不需要指定{}。


*****************************运算符 *******************************************
& 按位与运算符
| 按位或运算符
^ 按位异或运算符
~ 按位取反运算符
<< 3 左移3位，高位丢弃，低位补0
>> 3 右移3位，低位丢弃，高位补0
********************************逻辑运算符 a=10 b=20************************

and	    x and y	布尔"与" - 如果 x 为 False，x and y 返回 False，否则它返回 y 的计算值。	(a and b) 返回 20。
or   	x or y	布尔"或" - 如果 x 是非 0，它返回 x 的计算值，否则它返回 y 的计算值。	(a or b) 返回 10。
not 	not x	布尔"非" - 如果 x 为 True，返回 False 。如果 x 为 False，它返回 True。	not(a and b) 返回 False
*****************************input() raw_input()***********************************
input() ,raw_input() 用来获取控制台的输入。
raw_input() 将所有输入作为字符串看待，返回字符串类型。
注意：input() 和 raw_input() 这两个函数均能接收 字符串 ， python3使用 input()
而对于 input() ，它希望能够读取一个合法的 python 表达式，即你输入字符串的时候必须使用引号将它括起来，否则它会引发一个 SyntaxError 。

注意：python3 里 input() 默认接收到的是 str 类型。

函数语法
  input([prompt])  raw_input([prompt])
参数说明：
prompt: 可选，字符串，可作为一个提示语。
************************************* in ,not in ********************************
in 是一个判断元素是否包含在序列（如字符串、列表、元组、字典等）中的关键字。 not in 是不存在
语法： 3 in [2,3]   #判断3 是否存在于列表[2,3]中  输出True

----------------------------------------------------------------------------------------
循环； while 条件：                   while 循环的循环条件是自定义的，自行控制循环条件
       条件满足时，要做的事情1
       条件满足时，要做的事情2
python 代码如果一行写不完，代码后加\ (在代码中间按enter自动加入)  转入下一行，在代码中视为一行
"""
# print("helle",end='') 多个输出不换行， 后面加逗号 end=' ' 空字符                 换行 print()
#  制表符 tab  \t  print("abcd\tefg")   效果  abcd	  efg      可以使多行字符串对齐
#                  print("abcdef\tghijkm")    abcdef  ghijkm
#for循环  是一种轮询机制，即对一批内容追个处理 把数据集的数据挨个取出赋予临时变量；
#for line in list:       或  for line1,lin2,linn  in list #嵌套数组 同时取出第二层多个数据
# for 临时变量 in 待处理数据集（序列）   规范：临时变量只限定再循环内部使用 在循环外部也是可以访问的，（不规范不建议）
#   循环条件满足时执行的代码                    如果想在循环外使用，在循环外先定义变量，在循环内使用变量为临时变量
# continue 关键字用于中断本次循环，之间进入下一次循环 只对本循环内有效  临时中断
# break               直接结束循环 嵌套使用时本循环内有效               永久中断
#
# 数字序列：
# 一：range(num) 获取一个从0开始 到mum 结束 的序列（不含num本身）
# 二：range(num1,num2) 获取一个从num1到num2的序列（不含mum2本身）
# 三：range(mum1,mum2,step) step 是数字之间的步长，默认为1；
#随机数可以用 import random
#             mun = random.randint(1~10)
#安装库文件，用镜像站快， pip install pyinstaller -i https://pypi.tuna.tsinghua.edu.cn/simple/
"""
定义函数： def  函数命（传入参数）:          函数的调用：函数名（传入参数）    参数如果不需要可以省略，返回值也可省略；
               函数体                                                 函数是先定义后后使用；
               return 返回值
 传入参数的功能是：在函数进行计算时，接受外部（调用时）提供的数据   
  函数定义中提供的（x,y） 称为形式参数（形参） 表命函数声明将要使用2个参数 传入的时候，按照顺序传入参数
  函数调用中，提供的（5,8）  称为实际参数（实参）表示函数实际使用的参数值
  函数的返回值 可以用一个变量接收 变量 = 函数（实参）  函数体遇到 return 后返回值结束，后面代码不执行
  None 是类型“NoneType”字面量。表示空的， 没有return 语句，返回 None 或 return Noen 在if 语句总None 通 False
  在函数体内部定义变量为局部变量，在函数内部有效，用完销毁，在函数体外无法使用：在函数体定义的变量为全家变量
  如果想在函数体内修改全局变量，需要声明内这个内部变量为全局变量，即：global 变量 （—设置内部变量为全局变量）
"""
"""
 数据容器：一种可以存储多个元素的数据类型，
 --列表（list）变量名称 = [元素0,元素1,元素2,...] 反向[...元素-3，元素-2，元素-1] 取出列表元素：变量[1]
  -嵌套列表 [[元素0，元素...1][元素0，元素1，...]]           取出列表元素[1][3] (取出内部列表2的元素3)
  -查询列表元素下标： 列表名称.index(元素名)  返回值为该元素在列表的索引号（正向），没有该元素返回ValveError
  -修改特点位置（索引）的元素值： 列表[下标] = 值
  -插入元素：列表.insert(下标，元素)，在指定下标位置插入指定元素
  -追加元素：列表.append(元素)，将指定元素追加到列表的尾部
  -追加一批元素：列表.extend(其他数据容器)，将其他数据容器的数据取出，依次加到列表尾部。
  -删除元素 del 列表[下标]   无返回值
   -取出并删除元素：列表.pop(下标) 例如 x = 列表.pop(2)  列表用POP的方法取出列表第二个元素赋值给x 并删除该元素， POP 有返回值
  -删除某元素在列表的第一个匹配项：列表.remove(元素) 
  -清空列表内容：列表.clear()
  -统计某个元素在列表内的数量： 列表.count(元素
  -统计列表内有多少元素：len(列表)
  -列表的遍历： 将容器内的元素依次取出进行处理的行为，称为：遍历、迭代；
       index = 0
       While index < len(列表)          for 临时变量 in （数据容器）   
           元素 = 元素[idex]               对临时变量进行处理
           对元素进行处理
           idex += 1
   -While 循环可以自定义循环条件，并仔细控制                 --可以无限循环   -- 3适用于任何想循环的场所
   -for   循环不好用自定义循环条件 只可以一个个从容器内取出数据 --不可以无限循环 --适用于遍历容器场所或简单的固定次数循环场景
 
 --元组（tuple）：只读，不可更改(可以修改内部List) 的list, (元素0，元素1，...) 空元组: 
                 变量名称= （), 或= tuple()； 一个元素也要加逗号
   - 取出元素： num = 元组[下标] ，和list 方法一样
   - 查找元素下标 ： 元组.index(元素)
   - 统计某个元素出现的次数： 元组.count(元素)
   - 统计元组内元素的个数： len(元组)
 --字符串（str）：只读，字符串是字符的容器，一个字符串可以存放任意长度的字符； 变量 = “字符1字符2字符3.....” 
   - 取出字符： 字符 = 字符串[下标]
   - 字符串的替换：字符串.replace(字符串1,字符串2)，功能将字符串内的全部：字符串1替换为字符串2 ，
            注意: 不修改字符串本身，而是得到了一个新字符串 new_my_str = my_str.replace(str1,str2)
   -字符串的分割： 字符串.split(分隔符字符串（如空格符）)，功能：安装指定的分割字符串，将字符串划分为多个字符串，并存入列表对象中。
           注意：字符串本身不变，而是得到了一个列表对象；    
   -字符串的规整操作-（去前后空格和换行符）：字符串.strip()
                  -（去前后指定字符串）： 字符串.strip(字符串) 如 my_str = "12sra an bac21"  # 结果“sra an bac”
                                                          pint(my_str.strip("12")) #传入“12”，是1和2都会删除，是按照单个字符
   -查找给定字符的第一个匹配项的下标 ：字符串.index(给定字符串)      
   -统计字符串内某个字符串出现的次数 : 字符串.count(给定字符串)  
   -统计字符串的字符个数： 字符串.len() 
   -','.join('abc') 函数展开来写应该是str.join(item)，join函数是一个字符串操作函数  str表示字符串（字符），item表示一个成员，
          注意括号里必须只能有一个成员，比如','.join('a','b')这种写法是行不通的 
           示例：   ','.join('abc')  #result:  "a,b,c"  把一个字符串abd 用","切割成一个字符串"a,b,c"  
                                                                  如果"." 结果，"a.b.c"     
- 数据容器的切片： 对序列的操作不影响序列本身，而是得到一个新的（列表，元组，字符串）
   -序列：内容连续，有序，可使用下标索引的一类数据容器，列表，元组，字符串，均可视为序列；
   -切片:语法：序列[起始下标：结束下标：步长]        #从一个序列中，取出一个子序列；步长默认为1，可以不写
       -起始下标表示从何处开始，可以留空，留空视作从头开始；
       -结束下标（不含）表示何处结束，可以留空，留空视作截取的结尾 
       -波长表示，依次截取元素的间隔，1表示一个个取元素 2 每次跳过一个元素取，N 每次跳过N-1个元素取，-N 反向取
 --集合（set）不支持元素的重复（去重功能），元素是无序的,不支持下标索引访问，可以修改
   --定义集合变量 ：变量名称 = {元素1，元素2，...}  定义空集合： 变量名称 = set()
   -添加新元素： 集合.add(元素)；#结果：集合本身被修改，添加了新元素；
   -移除元素： 集合.remove(元素)
   -随机取出(并删除)一个元素:集合.pop() # 有返回值 取出后可以赋值个变量 原来集合里面的元素被删除
   -清空集合:集合.clear()
   -取两个集合的差集：集合1.difference(集合2) # 取出集合1和集合2的差集（集合1有而集合2没有的）,集合1和2不变，返回一个新集合
   -消除差集：集合1.difference_update(集合2) # 对比集合1和集合2，在集合1内，删除和集合2相同的元素。结果是集合1比修改，集合2不变
   -两个集合合并：集合1.union(集合2)  #集合1,2 合并，得到新集合，集合1,2不变
   -统计集合元素数量：len(集合)
   -集合的遍历，由于集合不支持下标索引，所以不支持While循环，支持for 循环遍历
 --字典（dict）:同样使用{}，不过存储的元素是一个个的：键值对， key不可以重复 不可以使用下标索引
   --定义字典变量 ：my_dict = {key:value,key:velue,.....} #空字典： my_dice = {} 或 my_dict = dict()
   --语法: value = 字典[key]
   --字典嵌套:key不可为字典，value 可以为任意数据类型
   -新增元素：字典[key]=value,  #字典被修改，新增了元素
   -更新元素：字典[kyt]=value, #字典被修改，元素被更新，key 不可以重复，对已存在的key执行上述操作，就是更新value值
   -取出并删除元素：value= 字典.pop(key) #获取指定key的value值，同时字典被修改，指定key的数据被删除  
   -清空字典：字典.clear()
   -获取全部key: keys = 字典.keys() #得到字典中的全部key
    获取全部velue:  velue = 字典.velues()  #得到字典中的velue
   -字典遍历   for key in keys:   #        #获取的keys 集合 依次取出赋值给key
                     print(my_dict1[key]) #输出全部value
      -- 第二种方法：for key in my_dict  #依次取出字典key值赋值给key变量
                           print(my_dict1[key])   #输出全部value
  -字典.items() 函数以列表返回可遍历的(键, 值) 元组数组   dict = {a:1,b:2}
                                                   print(dict.items()) #result:((a,1),(b,2))
         例如    for key,value in my_dict.items(): 可取出 全部key 和value
   --统计字典元素数量： len(字典)
 --数据容器的通用操作: 统计元素个数len(容器)；统计最大元素max(容器)；统计最小元素min（容器）
   -容器的转换 ：list（容器）   #将给定容器转换为列表      str(容器) #将给定容器转换为字符串
               tuplle(容器)   #将给定容器转化为元组      set(容器) #将给定容器转换为集合
               转列表: 元组后字符串转列表后元素是一个个字符， 字典抛弃value
               转字符串: 字典可正常显示 key value
   --通用排序功能： sorted(容器,reverse=True) #将给定容器进行排序默认从小到大；加上参数reverse（反转）=True（真） 排序从大到小 
              -排序后为列表   
      -两个字符串的比较大小 是取出两个字符串的一个个字符进行比较， 按照ASCII 码排序 靠前的大
""" 
****************************排序 sort*******************************************
list.sort()  函数： 这个方法会修改原列表，使其元素按指定顺序排列。默认从小到大
list.sorted() 函数 ：sorted() 函数返回一个新的列表，包含所有排序后的元素，原列表不变

sort() 函数用于对原列表进行排序，如果指定参数，则使用比较函数指定的比较函数。
list.sort(cmp=None, key=None, reverse=False)
参数
cmp -- 可选参数, 如果指定了该参数会使用该参数的方法进行排序。
key -- 主要是用来进行比较的元素，只有一个参数，具体的函数的参数就是取自于可迭代对象中，指定可迭代对象中的一个元素来进行排序。
reverse -- 排序规则，reverse = True 降序， reverse = False 升序（默认）。
*********************************************************************************************
"""
 --函数的进阶：
   --多个返回值：retrn 返回值1,返回值2，...... ； 接收变量1，接收变量2，..... = 函数（）：
   --函数参数种类
    -位置参数：调用函数时根据函数定义的参数位置来传递函数   def 函数（传参1，传参2，传参n)
                                                     函数（实参1，实参2，实参n) # 调用函数 
    -关键字参数：函数调用时通过“键=值”形式传递才是，              def 函数（传参1，传参2，传参n)
        -可以让函数更清晰，容易使用，同时也清除了参数的顺序需求；    函数（传参1=实参，传参=实参 传参n=s实参）#调用函数
        -注意：函数调用时，如果有位置参数时，位置参数必须在关键字参数的前面，但关键字参数之间不存在先后顺序，
    -缺省参数 (默认参数)：用于定义函数，为参数提供默认值，调用函数时可不传入该参数的值， def 函数（传参1，传参2，传参3=默认值）
                      ——（注意：所有位置参数必须出现在默认参数前，包括函数定义和调用）  函数（实参1，实参2）#实参3使用默认值
                      -默认参数必须写在后面
    -不定长参数(可变参数)：用于不确定调用的时候会传递多少个参数（不传参也可以）的场景
           --位置传递       def 函数（* 传参）    # 若干个传参，
                               变量= 传参       #传进的所有参数被变量收集，根据传参位置合并为一个元组（tuple）,
                           函数（实参1，实参2，实参n）#调用函数 实参个数没有限制
           --关键字传递: def 函数（**传参）    # 传入若干 "键=值" 参数
                         变量 =  传参        #接收 参数是“键=值”  同时根据“键=值”组成字典（dict）
                        函数（a=2,b=3,n=n)   #调用函数 
    --匿名函数：(临时使用移除)： lambda 传入参数：函数体（一行代码）                    def 函数1（函数2）
                              函数（lambda x,y : x+y） # 匿名函数作为参数传入函数        变量 = 函数2（参数1，参数2）
                                                                                     #下面匿名函数 = 函数2
                                                                         调用函数1：函数1（lambda x,y: x+y）
        -函数作为参数传递: dif 函数1（函数2）：           #函数2作为参数传入函数1
                             变量 = 函数2（实参1，实参2） # 调用函数2
                             print(变量)              
                         dif 函数2（x,y）:
                             return x+y      
                         函数1（函数2）        #调用函数1  结果是实参1+实参2
             -函数本身可以作为参数传入另一个函数中使用的
             -将函数传入的作用在于：传入计算逻辑，而非出入数据，
         
"""
"""  
     --文件
     打开文件：f = open(name,mode,encoding)      #可以打开一个已经存在的文件 或者创建一个新文件 f是open函数的文件对象
            -mane:文件名的字符串（可以包含路径）
            -mode:打开文件的模式（访问模式）：只读r，写入w，追加a
                -r 以只读方式打开文件，文件的指针将会放到文件的开头。这是默认模式
                -w 打开一个文件只用于写入。如果文件也存在则打开文件，并从头开始编辑，原有内容会被删除。
                                        如果该文件不存在，创建新文件
                -a 打开一个文件用于追加，如果该文件已存在，新的内容写入到已有内容之后，如果该文件不存在，创建新文件进行写入
            -encoding:编码格式（推荐使用UTF-8）
            例如 f = open("python.txt","r",encoding ="UTF-8")
             -- read()f方法：文件对象.read(num)； nun 表示要从文件中读取的数据长度（单位字节），如果没有num, 读取所有数据
                            多次调用reda()会从上一次结尾处开始
             --readlines(): 文件对象.readlines()； 一次性读取整个文件，并且返回（封装成）一个列表，其中每一行的数据为一个元素。
             --readline(): 文件对象，readline():  读取一行
             --关闭文件 文件对象.close()    #内置 flush()   即关闭并写入硬盘文件
             -- for循环读取文件 ： for line in open("python.txt","r“) # 把文件的每一行赋值给变量line
                                 print(line)                         #显示一行
             --with open 语法：  with open("python.txt","r")as f: #通过with open 的语句块 对文件进行操作 完成后自动关闭文件
                                     f.redalines()   
     写入文件: f.write("hell0 word")     直接调用write内容并未真正写入文件，而是会积攒在程序内存中，称之为缓冲区，
             -调用write 是文件不存在时创建文件，文件存在，会清空原文件,文件没关闭前 可以多次写入
     内容刷新：f.flush()                  当调用flush()的时候，内容会真正写入文件     （避免频繁操作硬盘，一次性写硬盘）
     time.sleep(时间)  #程序睡眠 多少秒
     文件追加：和写入类似，模式改为“a" , 文件不存在会创建文件，文件存在会在最后追加写入文件  
     with open(file_path, 'r', encoding="utf-8") as file 是一个上下文管理器语句，用于打开一个文件并确保文件在使用后会被正确关闭。
     这种语法是处理文件的首选方法，因为它可以自动处理文件的打开和关闭，即使在遇到错误时也是如此。                 
"""
"""
     --当程序遇到BUG那么接下来有两种情况：1.整个程序因为一个BUG 停止运行 2：对BUG进行提醒，整个程序继续运行，发生第一种情况，程序崩溃
        所以我们希望达到第二种情况，那这里我们就需要使用捕获异常；捕获异常的作用：提前假设某处会出现异常，提前准备，当出现异常，有后续手段
     --捕获常规异常：    
                   try:
                     可能出现的代码
                  except:  #(捕获全部异常)                          
                     如果出现异常执行的代码              
   --捕获指定异常  except NameError as e #捕获 NameError 异常 改异常NameError 为别名e
    -捕获多个异常  except (NameError,ZerDivision) : #多个异常写到括号里面（元组），用逗号分割
    -捕获全部异常 except Exception as e: 捕获全部异常 改异常名称为别名e  Except (顶级异常对象)
                  elst:                  #可选项 可以不行
                     没有出现异常执行的代码
                  finally:                #可选项，可以不行
                      有或者没有异常都要执行的代码        
"""
""" -- 模块的快捷导入 输入模块名 点击模块名 按Ait +Enter 然后选中要导入的包
    -- 模块的导入语法： [from 模块名] import [模块 | 类 | 变量 | 函数 | *] [as 别名]   #中括号表示可选
      -常用的组合方式：  as 别名 就是对这个功能（函数）改名字
         -- import 模块名      #import 翻译：导入        调用内部函数（类，变量）： 模块名.函数（实参）
         -- from 模块名 import 类、变量、方法等          #from 导入的模块 可以直接调用内部函数  不要写模块名
         -- from 模块名 import *
         -- import 模块名 as 别名
         -- from 模块名 import 功能名 as 别名      #从模块导入 一部分功能 
     自定义模块（python 文件）：创建一个python文件， 调用时用 import 文件名
       调试模块：
       -- if__main__ = __main__  变量 的功能是当程序是直接执行的才会进入if 内部，如果是被导入的，则if无法进入。
        - 直接输入main 自动变成 if__main__ = __main__
       -- 添加 __all__= [变量] 当使用 from xxx import * 导入时 只能导入这个变量列表的元素  只限于 improt * 有效
    ---
    python包：就是一个文件夹，内部包含了 一个 __init__.py 文件，该文件夹可包含多个python 模块
       -新建包  my_package                           #新建→python package→ 输入包名
       -新建包内模块： my_module1和my_module2
    --导入包：improt 包名.模块名
           - 使用：  包名,模块名.目标
    安装第三方包：命令提示符下 输入 pip install 包名称  ：即可通通过网络快速安装第三方包
            国内镜像站安装： pip install  -i https://pypi.tuna.tsinghua.edu.cn/simple 包名   #网址为清华站
      --在pycharm 中安装:settings → Project Interpreter → "+"→  (勾选options 填写 -i 镜像网址)
 ==常用模块 tkinter: 图形界面gui    系统模块：os  随机数可以用 ：import random  时间模块：import time
         
      
"""
""" json:
   json:一种轻量级的数据交换格式，采用完全独立于编程语言的文本格式来存储和表示数据（字符串）
    pythong 数据和Json 数据的相互转换：  
     import json            #导入json模块
     data = [{"name":"老王”,"age":16},{"name","张三","age",20] #准备符合格式json格式要求的python数据（列表）
    ---python 列表转换为json 字符串 (或字典转字符串)
     data = json.dunps(data) #通过json.dunps(data) 方法把python数据转换为json 数据
        data = json.dunps(data,ensure_ascii = False) # 数据里面有中文， ensure_ascii = False （不使用ascii码转换）
    ---json 字符串转换为python 列表 （或字符串转字典）
     data = json.loads(data) #通过json.loads(data) 方法把json数转换为python数据
   --BOM 字符 BOM是用来判断文本文件是哪一种Unicode编码的标记，其本身是一个Unicode字符（"\uFEFF"），位于文本文件头部。
      含义BOM字符 需要把编码改为 utf-8-sig
"""
"""pyecharts  （百度可视化 官网pyecharts.org 画廊地址：gallery.pyecharts.org)
   --全局配置 ：set_global_opts(参数)方法进行配置，
   --数据处理
   -时间线：Timeline()
"""
"""排序
   --列表的sort方法  ：用sort()函数对列表吧排序 并指定排序规则   
      - 使用方法： 列表.sort(key=选择排序依据的函数，reverse=True|False)
        - 参数key,要求传入一个函数，表示列表的每一个元素都传入函数中，返回排序的依据
        - 参数reverse,是否反转排序结果，True 表示降序，False 表示升序
        my_list= [["a",33],["b",55],["c",11]] 
        def choose_sort_key(element):
            return element[1]          #返回列表下标1
          my_list.sort(key=choose_sort_key(),reverse=True)  #根据列表下标1排序 降序  
         -- #采用匿名函数排序
        my_list= [["a",33],["b",55],["c",11]] 
        my_list.sort(key=lambda element: elenent[1],reverse=True)  #key =匿名函数
   ---sorted 排序:应用在所有可迭代的对象上
      语法：sorted(iterable, cmp=None, key=None, reverse=False)
      --iterable  可迭代对象
      --cpm 比较的函数，比较的对象从可迭代对象取出，大于返回+1，等于返回0，小于返回-1
      --key 用来比较的元素，只有一个参数，具体的函数的参数取自可迭代对象，指定可迭代对象中的一个元素进行排序
      --reverse  排序规则 True 降序，False 升序
  ----反转数据函数 reverse*()
    
        
"""
*********************************** Python中，星号（*）和双星号（**）是非常强大的工具 ****************************
1. 星号（*）
星号在Python中有两种主要用法：

• 解包（Unpacking）： 将一个可迭代对象（如列表、元组）解包成单独的元素。
  示例：    numbers = [1, 2, 3, 4]
           print(*numbers)  # 输出：1 2 3 4

• 可变数量参数： 在函数定义中，接收任意数量的位置参数。
  示例：def greet(*names):
            for name in names:
                print(f"Hello, {name}!")
        
        greet("Alice", "Bob", "Charlie")  
        # 输出：
        # Hello, Alice!
        # Hello, Bob!
        # Hello, Charlie!

2. 双星号（**）
双星号也有两种主要用法：

• 解包字典（Unpacking Dictionaries）： 将一个字典解包成关键字参数。
  示例： 
        def display_info(name, age):
            print(f"Name: {name}, Age: {age}")
        
        person = {"name": "Alice", "age": 30}
        display_info(**person) # 输出 ：Name: Alice, Age: 30

• 可变数量关键字参数： 在函数定义中，接收任意数量的关键字参数。
  示例：
        def show_details(**details):
            for key, value in details.items():
                print(f"{key}: {value}")
        
        show_details(name="Bob", age=25, city="New York")# 
        输出：   name: Bob
                age: 25
                city: New York
                
                
星号（*）和双星号（**）在Python中的用法十分灵活，它们不仅可以用于解包和传递可变数量的参数，还可以组合使用以处理复杂的函数参数
函数参数组合，你可以在同一个函数定义中同时使用星号和双星号，以接收不同类型的参数：
    def mixed_args(arg1, arg2, *args, **kwargs):
        print(f"arg1: {arg1}")
        print(f"arg2: {arg2}")
        print("args:", args)
        print("kwargs:", kwargs)
    mixed_args(1, 2, 3, 4, 5, name="Alice", age=30)
     #  arg1: 1
     #  arg2: 2
     #  args: (3, 4, 5)
     #  kwargs: {'name': 'Alice', 'age': 30}
函数调用时的解包 ，你可以在函数调用时同时解包多个可迭代对象和字典：
    def func(a, b, c, d, e):
        print(a, b, c, d, e)
    
    args = (1, 2)
    kwargs = {'c': 3, 'd': 4, 'e': 5}
    func(*args, **kwargs) 
    #输出 1,2,3,4,5


